// pages/corpus.js
import { Container, Typography } from '@mui/material';

const CorpusPage = () => (
  <Container>
    <Typography variant="h1" component="h2" gutterBottom>
      Corpus Structure
    </Typography>
    <Typography variant="body1">
      Details on the corpus structure and examples...
    </Typography>
  </Container>
);

export default CorpusPage;
